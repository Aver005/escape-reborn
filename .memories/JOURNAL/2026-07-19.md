# 2026-07-19

## Админская документация: `docs/guides/`

Написан полный набор гайдов по работе с мини-игрой через команды и конфиги.
Источник — чтение кода (`EscapeCommand`, `Arena`, `ArenaCheck`, `GameSession`,
`RespawnBlocks`, `OfflineGuards`, `Themes`, `GameEvent`, `ShopMenu`, реестры),
не пересказ старых доков. `docs/03-admin-and-arena-setup.md` описывает **оригинал
2021**, для reborn он неприменим — новые гайды его не наследуют.

Семь файлов:

| Файл | Роль |
|---|---|
| `guides/README.md` | Индекс, карта «задача → документ», топ-5 граблей |
| `guides/01-commands.md` | Справочник всех подкоманд: синтаксис, диапазоны, права, правила `ArenaCheck`, `debug` |
| `guides/02-configs.md` | Справочник всех файлов и ключей + правила безопасного редактирования |
| `guides/03-arena-create.md` | Создание арены, 3 сценария (маркеры / dynamicchests / клон) |
| `guides/04-arena-content.md` | Лут, контракты, темки, NPC, руды, рычаги |
| `guides/05-arena-tuning.md` | Тайминги, экономика, события, блоки возрождения, пресеты |
| `guides/06-operations.md` | Соло-тест, диагностика, восстановление после краша, ограничения |

## Что вскрылось попутно

- **[BUG] `ShopMenu` вешает сервер при >28 трейдов** — бесконечный `while` на
  main thread. Записан в `BUGS.md`, не исправлен.
- **Лимит 7 темок на NPC** (`ThemesMenu:66`) — тоже в `BUGS.md`.
- **Таймеры считаются по остатку `remaining`**, а не по прошедшему времени:
  число срабатываний = `floor((duration - 1) / interval)`. На дефолтах
  (1200/600) зарплата выдаётся **ровно один раз за матч** — вероятно, не то, что
  задумывалось. Требует решения владельца.
- **Дефолты дают WARNING валидатора**: `glowtime 600` при `duration 1200`
  попадает под правило «glow >= duration/2» (`ArenaCheck:105`). Каждая свежая
  арена создаётся с предупреждением.
- **`defaults` в `config.yml` не покрывает** `wear-min-percent`,
  `wear-max-percent`, `dynamic-chests` — `ArenaManager.applyDefaults` их не
  читает, новые арены всегда получают 40/90/false из полей `Arena`.
- **`fork-uses`**: `config.yml` = 4, но фолбэк `Arena.load` = 1. Расходится для
  рукописных `arena.yml` без ключа.
- **ID контрактов/темок/NPC регистрозависимы** (реестры ищут по точному ключу),
  а команды аплоуеркейсят аргумент → сущность с ID строчными буквами загрузится,
  но станет недоступна командам. ID арен, наоборот, нечувствительны.
- **Комментарий в `config.yml:37`** («Оффлайн-страж») стоит над секцией `match:`,
  а не над `offline-guard:` — смещён, путает.

## Не сделано

Гайды не проверялись на живом сервере — это документация, но команды и ключи в
ней сверены с кодом построчно. Дефекты `ShopMenu`/`ThemesMenu` не исправлены,
сознательно: задача была написать доки, не менять код.
