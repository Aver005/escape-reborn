# 2026-07-22 — Редизайн лута/сундуков (§17)

Полная замена системы лута со слов владельца. Спека утверждена поштучно
(14 edge-кейсов + контракты + рефилл), лежит в `PLANS.md` §17.

## Что сделано

**Модель.** Категория лута теперь ГЛОБАЛЬНАЯ (`loot/<id>.yml`), несёт И лут, И все
лимиты (вес 1–1000, min/max-per-chest, min/max-per-arena, min/max-chests,
refill-seconds). Пер-ареновый `loot.yml` и пер-ареновые `chest-categories.yml`
УБРАНЫ. Точка сундука несёт СПИСОК id категорий (0..N), формат `locations.yml`
`{location, categories:[...]}` (бэк-компат чтения одиночного `category`/плоского
Location). Контракты — свой конфиг в `arena.yml` (`contract-min/max-per-arena`,
`contract-min/max-per-chest`), кладутся независимо от лута.

**Спавн (GameSession, 3 фазы):** назначение точке ≤1 категории (Проход 1 —
минимумы по убыванию веса; Проход 2 — добор взвешенно; Проход 3 — филлеры
`max=-1`), все точки сперва воздух, выбранные → CHEST + лут. Бюджет
`max-per-arena` мягкий (`min-per-chest` жёсткий, кейс 10). Рефилл per-category,
аддитивный (докидывает в свободные слоты, старое лежит), триггер по первому
забору. Динамические сундуки — категория с наибольшим весом. `giveRandomLoot` —
взвешенный выбор глобальной категории.

**GUI** `/escape loot [gui]` — экран категорий (список + создать из
инвентаря/сундука/копией + удалить) → редактор категории (настройки кликом,
сетка предметов). Текст-команды `/escape loot list|create|delete|name|icon|weight|
minchest|maxchest|minarena|maxarena|minchests|maxchests|refill`. Мастер-жезл
`/escape chestsetup` — ПКМ ТОГГЛИТ категорию на точке (мультивыбор), стрела =
следующая точка (`NEXT_TAG`). `/escape chesttag` — глобальные метки, тоггл.

**Миграция** (`loot/LootMigration`, одноразовая, маркер `loot/.migrated`):
старые `arenas/<id>/loot.yml`+`chest-categories.yml` → глобальные
`<arena>_<oldcat>` с пулом арены как лутом; точки ремапятся. Плоские старые
точки (в старой модели = «default») привязываются к `<arena>_default`, если та
категория есть (иначе остаются без категории).

## Как делалось

Ядро (LootCategory, LootCategoryRegistry, Arena, EscapePlugin) — вручную с
зафиксированным API. Остальное — 5 параллельных субагентов по непересекающимся
файлам против контракта: A=GameSession, B=GUI(+5 меню-классов), C=миграция,
D=мастер-жезл, E=SetupListener+ArenaCheck. Координатор (EscapeCommand,
messages.yml, NEXT_TAG-роутинг, straggler в ArenaManager) — вручную. Все ключи
игрокам собраны из отчётов агентов в `messages.yml`.

## Проверка

- `./gradlew build` — BUILD SUCCESSFUL с первого раза (весь код агентов собрался
  вместе против контракта; IDE-диагностика по ходу была environmental-шумом —
  сломанный classpath JDK/Bukkit).
- Смоук на `E:\Servers\escape`: `Escape enabled, arenas loaded: 2`, миграция
  `Arenas processed: 2, categories created: 4`, `Done ~12s`, 0 стектрейсов.
- Миграция вживую: CLASSIC → 4 категории (classic_common/default/hotspot/rare)
  с лутом/лимитами/рефиллом; точки 12 common + 22 hotspot + 63 rare +
  **42 default** (плоские, восстановлены фиксом), 0 пустых. ALCATRAZ — 185 точек
  пусты (не было пула/категорий → нужна ручная разметка `/escape loot` +
  `chesttag`/`chestsetup`).

## Дефект по ходу и фикс

Первый прогон миграции: плоские старые точки CLASSIC (42) стали ПУСТЫМИ вместо
`classic_default` (мой новый `readChestSpots` даёт плоской точке пустой список, а
`defaultId` не выставлялся, когда у арены была явная старая категория «default»).
Фикс в `LootMigration`: (1) старая категория с id «default» запоминается как
`defaultId`; (2) пустой список точки → `defaultId`, если он есть. Перепроверено
сбросом состояния (возврат старых id + `rm loot/`) и повторным смоуком — 0 пустых
на CLASSIC.

## Правка после плейтеста: СОВМЕЩЕНИЕ категорий (не выбор одной)

Владелец поиграл: если у сундука несколько категорий, выбор ОДНОЙ случайной давал
«в редком сундуке одна вещь» (редкая категория диктует min/max-per-chest=1). Новое
поведение — **совмещать**: сундук наполняется лутом из ВСЕХ своих категорий, редкая
добавляет предмет ПОВЕРХ базовой.

Изменения только в `GameSession` (модель упростилась — 3-фазное присвоение одной
категории убрано):
- `activeChestCategory: Map<Location,String>` → `activeChestCategories: Map<Location,List<String>>`.
- `placeChests`: каждая категория НЕЗАВИСИМО берёт свои сундуки в [min,max]-chests из
  своих точек (max=-1 → во всех своих точках); точка, выбранная несколькими → совмещение.
- `fillChestInitial`/`fillChestRefill` принимают `List<LootCategory>`, наполняют по весу
  DESC (весомая гарантирует свой per-chest минимум первой, когда слоты кончаются, cap 27).
  Бюджет max-per-arena по-прежнему per-category и мягкий.
- Рефилл: таймер сундука = минимальный положительный `refill-seconds` среди его категорий;
  на срабатывании докидывает из всех.
- Динамический сундук — по-прежнему одна категория (наибольший вес), но хранится списком.
- `weightedPickCategory` остался только для `giveRandomLoot` (награда события).
- Вес категории теперь: (1) порядок наполнения в совмещённом сундуке, (2) выбор
  категории динамического сундука и награды. Для присвоения точкам он больше НЕ нужен.

Собрано (build зелёный), задеплоено, смоук чист. Логика совмещения — на плейтест.

## Правка 2: раздача контрактов (финальная модель)

Итерации: (а) старая пер-арена «по 1 на сундук до max-per-arena» — над сотней
сундуков призрак; (б) пер-сундук `[min,max]-per-chest` — владелец: «система с 1/5
шанса ерунда» + контракты всегда в первых слотах. Финальная модель со слов
владельца:

**Старт матча** (`placeContracts`): катаем случайный `total` на матч в
`[min-per-arena, max-per-arena]` (`max=-1` → до предела `chests*max-per-chest`).
Раскидываем поштучно по **СЛУЧАЙНЫМ** сундукам из пула в **СЛУЧАЙНЫЙ** свободный
слот; сундук, набравший `max-per-chest` (или без свободных слотов), выбывает из
пула. `min-per-chest` в раздаче больше НЕ используется (оставлен в конфиге для
совместимости).

**Рефилл** (таймерный и «ключик»): в каждом сундуке при рефилле шанс **1/4**
добросить один контракт в случайный слот (`maybeRefillContract`) — как в
оригинале.

Слот теперь всегда случайный (`placeContractInChest` собирает свободные слоты и
берёт случайный, не `firstEmpty`). Дефолты контрактов: `min/max-per-arena 4/16`,
`max-per-chest 3`. Плотность на старте задаёт `total` (per-arena), потолок в
сундуке — `max-per-chest`. Собрано+задеплоено (смоук по просьбе владельца не
гоняли — джарник на `E:\Servers\escape`, активируется рестартом). Проверка на
матче: `/escape debuglog on` → `contracts-placed ... placed=N total=N`.

## НЕ проверено (нужен плейтест владельца — как всегда в этом проекте)

3-фазный спавн вживую (мин/макс сундуков, филлеры, бюджеты), рефилл по первому
забору + аддитивность, раздача контрактов, GUI (создание из инвентаря/сундука/
копии, правка лимитов), мастер-жезл мультитоггл, chesttag-тоггл, динамические
сундуки с наибольшим весом. ALCATRAZ требует разметки категорий.

## Правка 4: наполнение содержимого из 3 источников (лут + жители)

Со слов владельца: категорию лута можно было только СОЗДАТЬ из трёх источников
(инвентарь / просматриваемый сундук / копия). Добавлена возможность ДОПОЛНИТЬ и
ЗАМЕНИТЬ (полностью) содержимое теми же тремя способами — и такой же набор для
жителей (создание + дополнить/заменить товары; владелец выбрал полный паритет).
Замена применяется сразу, но пустой источник (пустой инвентарь / не-сундук или
пустой сундук / пустая категория-источник) НЕ стирает цель — guard. Вес предмета
в GUI по-прежнему НЕ редактируется: из инвентаря/сундука вес=1, копия/источник
вес сохраняет (решение владельца отдельным пунктом).

**Лут.** В редакторе категории (`LootCategoryMenu`) новая кнопка «Наполнение»
(слот 51, HOPPER) → `LootFillMenu`: 6 опций (дополнить/заменить × инвентарь/
сундук/категория). Источник-категория → `LootFillSourceMenu` (пикер, цель
исключена, веса сохраняются). `LootCreate` получил списковые хелперы
`lootFromInventory`/`lootFromChest` (переиспользуются создателем и наполнением).

**Жители (полный паритет).** Новый глобальный список жителей: `/escape trades`
БЕЗ VID → `TraderListMenu` (иконки-яйца, роли магазин/смотрящий/мусорщик, число
товаров) с кнопкой «Создать» → `TraderCreateMenu` (инвентарь/сундук/копия; id
авто «VILLAGER…»; цена товаров = 1). `/escape trades <VID>` как раньше открывает
редактор товаров. В редакторе (`TradeListEditorMenu`) новая кнопка «Наполнение»
(слот 51) → `TradeFillMenu` (те же 6 опций; источник-житель → `TradeFillSourceMenu`).
Модель: `TraderType.copyAs` (глубокая копия имя+товары+темки+скрап),
`TraderRegistry.all()/isEmpty()`. Helper `TradeCreate` (симметричен `LootCreate`).

**Команда.** `trades` вынесен из id-switch в ранний switch (до гейта args),
рядом с `loot`: `handleTrades(p,args)` — нет VID → список, есть → редактор.
Tab-complete не тронут (`trades` уже в ADMIN_SUBS и трейдер-группе).

**Новые файлы (8):** `menu/LootFillMenu`, `menu/LootFillSourceMenu`,
`menu/TradeCreate`, `menu/TraderListMenu`, `menu/TraderCreateMenu`,
`menu/TraderCopySourceMenu`, `menu/TradeFillMenu`, `menu/TradeFillSourceMenu`.
**Правки:** `menu/LootCreate`, `menu/LootCategoryMenu`, `menu/TradeListEditorMenu`,
`trader/TraderType`, `trader/TraderRegistry`, `command/EscapeCommand`,
`resources/messages.yml` (секции `loot-fill`/`trade-fill`/`trader-list`/
`trader-create` + кнопки `loot-category.btn-fill`, `trade-list.fill`).

**Проверка.** `./gradlew build` — BUILD SUCCESSFUL с первого раза. Смоук/плейтест
НЕ делались (решение владельца — только сборка). На плейтест: обе кнопки
«Наполнение», 6 режимов ×2 системы, guard пустого источника (замена не стирает),
копия сохраняет вес/цену, `/escape trades` без VID открывает список и создаёт
жителя, авто-id + переименование `/escape villagername`.

Грабля на будущее: сбор из инвентаря берёт `getContents()` (41 слот — вкл.
броню/офхенд), как и существующее создание лута; админ не должен быть в броне
при сборе из инвентаря.

---

# 2026-07-22 — Правка 5: стыки решёток при восстановлении + ориентация сундуков

Два бага со слов владельца.

## 1. Соседние решётки «рвутся» после восстановления
Когда игрок выбивает прут решётки, ваниль разрывает стык у СОСЕДНЕГО уцелевшего
прута (обрубок). `cleanup` возвращал только выбитые блоки (editedBlocks); у
соседа стык не чинился. Второй проход `setBlockData(data, true)` тут не помогает:
повторная установка НЕИЗМЕНЁННОГО BlockData — no-op в движке (LevelChunk отдаёт
early-return, соседу физика не уходит). Грабля на будущее: физикой соседний
connecting-блок пересчитать нельзя, если сам блок не меняется.

Фикс — `GameSession.fixConnectingNeighbors(restoredBlocks)` (третий проход в
cleanup): для каждого восстановленного `MultipleFacing` (решётки/панели/заборы)
смотрим горизонтальные стороны, куда он тянется (`hasFace`); если сосед — тоже
`MultipleFacing`, в эту сторону НЕ соединён и НЕ восстанавливался сам (нет в
editedBlocks), точечно ставим ему встречный face (`setFace(opposite, true)`,
физика не нужна — правим ровно ту грань). Зеркалим реальный стык, вани-правил
соединения не переизобретаем. Стены (`Wall`, height-based) не трогаем — жалоба
была про прутья.

## 2. Сундуки всегда смотрели на север
`setType(Material.CHEST)` всегда даёт NORTH, а сторона нигде не хранилась.
- **Хранение:** новое поле `Arena.chestFacings` (Map<Location, BlockFace>),
  сериализуется опциональным `facing:` в записи точки `locations.yml`
  (`{location, categories, facing}`). Нет `facing` → NORTH (полный бэк-компат).
  `getChestFacing(loc)` — с дефолтом NORTH.
- **Захват стороны:** (а) при установке маркера `/escape addchest` —
  `SetupListener.onPlace` берёт `getBlockPlaced().getBlockData().getFacing()`
  (в BlockPlaceEvent блок уже стоит — см. LEARNINGS); (б) при отметке категории
  `chesttag` — синхронизируем из сундука-точки в мире (источник истины).
- **Применение:** `SetupMarkers.placeChest` и `GameSession.placeChests` после
  постановки CHEST разворачивают его в сохранённую сторону (Directional).
- **Команда** `/escape chestface <ID>`: смотришь в упор на сундук-точку → он
  разворачивается лицом К тебе (`p.getFacing().getOppositeFace()`, как ванильная
  установка), сохраняется в арену и сразу применяется к блоку-маркеру.
  Сообщения `chestface.not-a-point|set`. Удаление точки чистит и facing.

## Статус
`./gradlew build` зелёный. Смоук/плейтест НЕ делались (решение владельца —
только сборка). Проверить в игре: побег через решётку рядом с уцелевшей секцией
(после матча стыки целые), и что сундуки на арене встают как поставлены + команда
поворота.
