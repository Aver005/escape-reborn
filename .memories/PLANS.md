# PLANS — роадмап

Last updated: 2026-07-21 — пакет из 9 доработок (админ-GUI + геймплей), собрано
и задеплоено, смоук не прошёл (живой сервер держит локи). Фокус прежний: плейтест.

## Ближайшее

- `[TODO]` **Пакет 2026-07-21 — проверка вживую** (собрано, смоук НЕ прошёл —
  живой сервер держит файловые локи; детали `JOURNAL/2026-07-21` разделы 3-4):
  1. **GUI Мусорщика** `/escape scrapedit <VID>` — клик крутит цену, порог
     износа, Shift-клик по вещи в инвентаре добавляет материал (пагинация при
     >45 материалах); персист в traders.yml;
  2. **GUI торгов** `/escape trades <VID>` — цена кликом, Q убирает, «добавить»
     открывает старое меню; пагинация при >45 товарах;
  3. **GUI точек жителей** `/escape villagers <ID>` — ЛКМ телепорт к точке, ПКМ
     редактор торгов типа точки; блок при матче;
  4. **отметчики** `/escape chesttag <ID>` — ПКМ и слом сундука-точки присваивают
     категорию без мастера; чужая арена/не-точка/мёртвая категория — отказ;
  5. **стыки прутьев** — сломать прут в ряду, после матча ряд цел, не пролезть;
  6. **уникальные спавны** — 2+ игрока на разных точках, финалка тоже разводит;
  7. **traderquota** — лимит по типу соблюдается, точки случайны, `-1` снимает,
     тип без квоты = все точки, пустые квоты = прежний `trader-count`;
  8. **огонь** — огниво поджигает, не ползёт/не жжёт, гаснет 12–30 сек, рукой
     тушится (кроме огня карты), после матча чисто, `fire.enabled: false` = запрет;
  9. **breakable** — метчик помечает дверь/кровать/стену, в матче ломается, после
     матча ВСЁ вернулось (обе половины), стыки целы.

- `[TODO]` **Плейтест на реальной карте**: собрать арену маркерами, прогнать
  полный матч 2+ игроками, проверить чек-лист из STATE.md («написано, но не
  проверено»). Приоритеты проверки:
  1. полный цикл блока возрождения (установка→смерть→5 сек→респаун→прокачка
     до изумруда→слом врагом→аннуляция) + срыв возрождения;
  2. оффлайн-стражи (выход→возврат при страже / убийство стража / возврат на
     блок / просрочка; вид моба = уровень блока);
  3. темки: взять/бросить/кулдаун, сдача SELF/ANY/адресату, передачка через
     труп, доставка с изъятием, «Волшебный ключик» по сундуку;
  4. лут: случайный износ (wearmin/wearmax), рукописный loot.yml
     (docs/examples/loot-prison.yml), динамические сундуки (`dynamicchests 1`,
     откат содержимого после матча);
  5. Кровавая луна (ночной старт), Туман (корточки), `/escape worldsetup`.
  6. улучшенные механики: снежки/яйца (урон доходит и не задваивается, броня
     режет, кредит убийце, окно неуязвимости не даёт спамить, куры не плодятся),
     удочка-крюк (рывок без зачарки и с «Силой» 1–5, дуга, крючок не зависает,
     падение после крюка засчитывается рыбаку).
  7. **логирование**: `/escape debuglog on` ПЕРЕД матчем, после — `save`.
     Сохранённый лог и есть протокол плейтеста: по нему видно и порядок
     событий, и причины отказов, и что мир откатился (`blocks-restored`,
     `entities-removed` в конце).
- `[TODO]` **Касты — проверка вживую** (собрано 2026-07-20, смоук не делался):
  выбор в лобби (сундук-селектор → GUI, «Без касты»/«Случайная», подсветка
  выбранного), выдача на старте (системка + золото каста + предметы, броня
  авто-надета, всё почти сломано), дефолт для не выбравшего, `/escape kit copy`
  глобального на арену → правка копии не трогает `kits.yml`, `/escape kit edit`
  (GUI-редактор), name/icon/gold/default. Баланс 5 стандартных — по ощущениям.
- `[TODO]` **Мусорщик — проверка вживую** (собрано 2026-07-20, смоук не делался):
  ПКМ по NPC с ролью scrap → приёмка, продажа сломанного (цена растёт с износом),
  «Сдать весь хлам», отказ годного (< порога) и системки (вилка/блок/контракты),
  совмещённый NPC (магазин+темки+scrap → меню из 3 кнопок, «назад» работает),
  `/escape addscrap|scrapwear|scraplist`, пример типа `МУСОРЩИК`.
- `[TODO]` **Модификаторы сессии — проверка вживую** (собрано 2026-07-20, смоук
  не делался): бюллетень в лобби (ПКМ переключает голос, счётчик «за/нужно»
  обновляется у всех), решение на старте (>1/3 → принят, объявление в чат),
  эффекты каждого из 7 режимов реально крутят числа (зарплата/события/золото/
  износ/скрап/рефилл/сундуки/ночь), при пустом `modifiers.yml` фича спит.
- `[TODO]` **Категории сундуков + мастер — проверка вживую** (§16, собрано
  2026-07-20, смоук не делался, сервера на машине нет): миграция старой арены
  (грузится, `/escape check` даёт fallback-сводку, матч как раньше);
  `chestcat copy/quota/loot/refill/icon/name` → персист в
  `arenas/<ID>/chest-categories.yml`; мастер `chestsetup <ID>` (стеш инвентаря,
  ADVENTURE, жезлы, телепорт по точкам, ПКМ = категория+переход, финиш/барьер/
  `stop` = возврат инвентаря, дисконнект+заход = onJoin-восстановление),
  `chestsetup <ID> gui` (пагинация, клик = прыжок); guard входа в арену под
  мастером; матч в quota (DebugLog `chests-cat`: кол-во ~ `min(round(quota*mult),
  точек)`, лут в пределах loot-min/max, 0-refill не рефиллится, ключик рефиллит),
  `abundance/scarcity` крутят квоты; ArenaCheck-квота (сироты/перекос квот);
  cleanup — мир чист, второй матч без залипшего стейта.
- `[IDEA]` Стата продаж Мусорщику (`junk_sold`) — потребует миграции SQLite
  (COLUMNS/Row/CREATE+ALTER/UI). Отложено, владелец не просил.
- `[IDEA]` Модификатор «перекос лута» (Оружейная/Аптека) — нужна категоризация
  предметов по типу; отложено (в v1 модификаторов только числовые эффекты).
- `[TODO]` По итогам плейтеста — балансные правки (duration/интервалы/износ
  уже крутятся конфигом per-arena; золото/предметы кастов — в kits.yml;
  база скрапа Мусорщика — в traders.yml).

## Потом (без владельца не начинать)

- `[IDEA]` Таймер на табличке пустого сундука («Пустой • 2:31») — владельцу
  предложено, решения нет.
- `[IDEA]` Per-item диапазоны износа в loot.yml (`wear: 70-95`) — предложено.
- `[IDEA]` Страховочный скан пустых незакрытых сундуков из главного тика
  (сценарий «опустошил и вышел» — рефилл не планируется).
- `[IDEA]` Живой счётчик прогресса DELIVERY/COURIER в лоре меню темок.
- `[IDEA]` Журналирование editedBlocks на диск — откат мира после краша
  (BUGS.md).
- `[IDEA]` Топы по статистике (SQLite уже хранит всё нужное), placeholder-ы.
- `[IDEA]` BungeeCord/мультисервер, если игра поедет на сеть.
- Решено **НЕ делать**: событие «Амнистия» (07-decisions §6, N7).
  ~~NPC с задачами~~ — решение §4 ОТМЕНЕНО владельцем: сделано как NPC 2.0
  «Смотрящие» (§10).
