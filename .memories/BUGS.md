# BUGS — известные дефекты и ограничения

Last updated: 2026-07-19

Плейтеста ещё не было (см. STATE.md). Один дефект найден чтением кода при
написании `docs/guides/`.

## [BUG] Зависание сервера: >28 товаров у типа NPC

`ShopMenu` (конструктор) при открытии магазина уходит в **бесконечный цикл на
main thread**, если у типа NPC больше 28 трейдов.

→ `menu/ShopMenu.java:54-58`

```java
while (isBorder(slot)) {slot++;}          // slot >= size-9 => isBorder всегда true
if (slot >= inventory.getSize()) {break;} // недостижимо: while не выходит
```

`isBorder` (`:82`) возвращает `true` для любого `slot >= size - 9`, поэтому
`while` крутится вечно, а проверка выхода стоит **после** него. `rowsFor` (`:76`)
кламплен шестью рядами → полезных слотов ровно 28 (10-16, 19-25, 28-34, 37-43).
28 трейдов — ок, 29-й вешает сервер при первом же ПКМ по такому NPC.

Ни `TradeEditorMenu`, ни `ArenaCheck` размер ассортимента не ограничивают.

`[TODO]` Починить: поменять порядок — сначала проверка `slot >= size`, потом
`while`; либо считать пул допустимых слотов заранее. Плюс WARNING в `ArenaCheck`
при `trades.size() > 28`. Пока не исправлено — ограничение задокументировано в
`docs/guides/04-arena-content.md`.

## Ограничения by design

- **Максимум 7 темок на NPC**: `ThemesMenu` рендерит слоты 10-16 и делает
  `break` при `slot > 16` — восьмая и далее не показываются без предупреждения,
  сданные темки продолжают занимать слоты. → `menu/ThemesMenu.java:66`.
- `[?]` **Двойные сундуки не поддержаны**: LOOT-прогресс, FIND-пересчёт и
  рефилл ловят только `holder instanceof Chest` → точки сундуков не ставить
  вплотную друг к другу. → `listener/GameListener.onInventoryOpen/Close`.
- **Защита от огня/листвы действует на весь мир арены** (как в оригинале) —
  аренам нужны выделенные миры, не основной выживач. → `ProtectionListener`.
- **Спавнов должно быть ≥ игроков**: при нехватке точки переиспользуются
  (шафл по кругу), игроки могут заспавниться вместе. → `GameSession.startMatch`.
- **Кредит убийства** — окно 10 сек после последнего удара (`KILL_CREDIT_MILLIS`);
  смерть от падения без чужого урона убийцей не считается.
- **Restart сервера посреди матча**: мир НЕ откатится автоматически (editedBlocks
  в памяти). Игроки восстановятся при заходе (снапшоты на диске), но сломанные
  решётки/выставленные сундуки останутся до ручной чистки или следующего матча
  на тех же точках. `[IDEA]` — журналить editedBlocks на диск.

## Наследие оригинала

16 багов оригинала задокументированы в `docs/05-quirks-bugs-unfinished.md` и
в reborn **исправлены** (`docs/08-reborn-guide.md`, раздел «Отличия»). При
сверке поведения с .old/ не переноси их обратно.
